using UnityEngine;

public class TransformResetter : MonoBehaviour
{
    [SerializeField] private bool noPos, noRot, noScal;
    public bool NoPos => noPos;
    public bool NoRot => noRot;
    public bool NoScal => noScal;
    // Start is called before the first frame update
}
