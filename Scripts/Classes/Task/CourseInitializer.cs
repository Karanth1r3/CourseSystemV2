
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace course
{
    public class CourseInitializer : MonoBehaviour
    {
        [SerializeField] TaskWithOptions[] initiallyActive;
    // Start is called before the first frame update
    void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}

